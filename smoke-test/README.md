# Smoke Test

This folder is a standalone consumer check for Alexium before publishing or pushing the repository.

Run:

```bash
node src/index.js
```

It exercises the core, backend, frontend, logger, plugin, and utility packages the way an external project would consume them.
